name: Deploy CloudFormation Stack

on:
  push:
    branches:
      - master  

jobs:
  deploy:
    runs-on: ubuntu-latest    

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.ACCESS }}
          aws-secret-access-key: ${{ secrets.SECRET }}
          aws-region: eu-central-1

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip
          sudo pip3 install awscli

      - name: Deploy CloudFormation stack
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.ACCESS }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SECRET }}
          AWS_REGION: eu-central-1
          STACK_NAME: "mini-civilization2"
        run: |
          aws cloudformation create-stack \
          --stack-name $STACK_NAME \
          --template-body file://${{ github.workspace }}/deploy.yml \
          --capabilities CAPABILITY_NAMED_IAM \
          --debug
